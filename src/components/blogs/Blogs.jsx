import React from 'react';
import PropTypes from 'prop-types';

const Blogs = props => {
    return (
        <div>
            <h1>hello form blogs</h1>
        </div>
    );
};

Blogs.propTypes = {
    
};

export default Blogs;